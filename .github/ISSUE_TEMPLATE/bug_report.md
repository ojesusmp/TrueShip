---
name: Bug report
about: Report something that does not work as documented.
title: "[Bug] "
labels: bug
assignees: ojesusmp
---

## Summary

A one-line description of the bug.

## Environment

- trueship version: (run `npm view @ojesusmp/trueship version` or check `package.json`)
- Install method: (npm / git / Claude marketplace)
- Operating system: (Windows / macOS / Linux + version)
- Node version: (run `node --version`)
- git version: (run `git --version`)
- npm version: (run `npm --version`)

## Steps to reproduce

1. ...
2. ...
3. ...

## Expected behavior

What you expected to happen.

## Actual behavior

What actually happened. Include the assistant turn that misbehaved, plus the resulting `git status` and `npm publish` output if relevant.

## Files changed by TrueShip during the failing run

If TrueShip wrote or modified files before failing, list them so the maintainer can understand the partial state.

## Additional context

Anything else that might help — related issues, recent changes to your environment, etc.
